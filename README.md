# KareonLogistic

App (Next.js) deployada en Vercel. Releases automáticos desde `main` con semantic-release.

## Requisitos
- Node.js >= 20
- npm >= 10

## Setup
```bash
npm install
cp .env.example .env
npm run dev
